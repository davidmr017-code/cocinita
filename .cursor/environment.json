{
  "name": "Cocinita",
  "install": "npm ci && npm --prefix server ci && npm run build",
  "terminals": [
    {
      "name": "web",
      "command": "npm run dev:local",
      "description": "Frontend Vite en http://localhost:5173"
    },
    {
      "name": "api",
      "command": "npm run dev:api",
      "description": "API Express en http://localhost:3080 (requiere DATABASE_URL y JWT_SECRET)"
    }
  ]
}
